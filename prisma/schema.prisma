datasource db {
  url      = "file:./db"
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Note {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  text     String?
}